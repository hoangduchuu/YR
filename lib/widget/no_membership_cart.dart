
import 'package:flutter/material.dart';

class NoMemberShipCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      child: Center(
        child: Text("No MemberShip Card"),
      ),
    );
  }
}
